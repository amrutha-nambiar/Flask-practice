from flask import Flask

app = Flask(__name__)

# 1. Greeting
@app.route('/hello/<name>')
def greet(name):
    return f"Hello {name}!"

# 2. Sum
@app.route('/add/<int:a>/<int:b>')
def add(a, b):
    return f"Sum is {a + b}"

if __name__ == '__main__':
    app.run(debug=True)
